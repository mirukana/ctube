<nav data-embedded="{{embedded}}">
    <form action="/results">
        <input
            type="search"
            name="search_query"
            placeholder="Search"
            value="{{ search_query }}"
            autocomplete=true
            autofocus=true
        >
    </form>

    <button class="previous" type="submit" name="page" value="{{ prev_page }}">
        <a class="qutebrowser" href="{{ prev_url }}">Previous</a>🡠
    </button>

    <span class="page-num" data-num="{{page_num}}">{{ page_num }}</span>

    <button class="next" type="submit" name="page" value="{{ next_page }}">
        <a class="qutebrowser" href="{{ next_url }}">Next</a>🡢
    </button>
</nav>
