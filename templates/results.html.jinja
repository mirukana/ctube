<!DOCTYPE html>
<html>
    <head>
        <title>{{ query }}</title>
        <meta charset="utf-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1, maximum-scale=1">
        <link rel="stylesheet" href="static/css/main.css">
    </head>
    <body class="thumb-grid">
        {% for entry in entries %}
            <iframe
                title="{{ entry.title }}"
                src="{{ entry.preview_url }}"
                scrolling="no"
                loading="lazy"
            ></iframe>
        {% endfor %}
    </body>
</html>
